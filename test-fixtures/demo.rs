//! Sample code for annot onboarding
//! Try annotating these lines!

fn greet(name: &str) -> String {
    format!("Hello, {}!", name)
}

fn main() {
    let message = greet("World");
    println!("{}", message);
}

// TODO: Add more examples?
// Try using /replace to suggest changes!
